enum OrgType {
  ESTATE
  COOPERATIVE
  GYM
  SCHOOL
  CLINIC
  OTHER
}


enum CycleFrequency {
  MONTHLY 
  YEARLY
  ONE_TIME
  CUSTOM
}


enum CycleStatus {
  OPEN
  CLOSED
  ARCHIVED
}

enum ChargeStatus {
  PENDING
  PAID
  OVERDUE
  UNDERPAID
  OVERPAID
}

enum PayoutStatus {
  PENDING
  COMPLETED
  FAILED
}

enum MemberStatus {
  ACTIVE
  INACTIVE
}

enum CollectionStructure {
  FLAT
  VARIABLE
}

enum ConversationRole {
  ADMIN
  MEMBER
  NEW
}

enum ConversationStep {
  AWAITING_ORG_NAME
  AWAITING_COLLECTION_NAME
  AWAITING_ORG_TYPE
  AWAITING_CYCLE
  AWAITING_STRUCTURE
  AWAITING_FLAT_AMOUNT
  AWAITING_FEE_LINES
  AWAITING_ONE_TIME_DEADLINE
  AWAITING_CUSTOM_DEADLINE
  AWAITING_PAYOUT_BANK
  AWAITING_PAYOUT_CONFIRM
  AWAITING_PAYOUT_AMOUNT 
  AWAITING_WEB_EMAIL
  AWAITING_WEB_EMAIL_CONFIRM
  AWAITING_MEMBERS
  AWAITING_NEWCYCLE_DEADLINE
  COMPLETE
  MEMBER_AWAITING_NAME
  MEMBER_AWAITING_PLAN_SELECTION
  MEMBER_AWAITING_CONFIRM
  MEMBER_COMPLETE
}

enum WebhookReconciliationStatus {
  SUCCESS
  UNDERPAYMENT
  OVERPAYMENT
  PAYMENT_FAILED
  PAYOUT_SUCCESS
  PAYOUT_REFUNDED
}