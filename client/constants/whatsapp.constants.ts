// Twilio WhatsApp Sandbox — members must opt in with the join phrase once
// before the bot can message them, then they can text their invite code.
export const WHATSAPP_JOIN_NUMBER = "+14783758457";
export const WHATSAPP_JOIN_CODE = "join strange-spring";
